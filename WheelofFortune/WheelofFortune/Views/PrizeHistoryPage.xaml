<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WheelofFortune.Views.PrizeHistoryPage"
             xmlns:viewmodels="clr-namespace:WheelofFortune.ViewModels"
             xmlns:model="clr-namespace:WheelofFortune.Models;assembly=WheelofFortune">
    <!--Databinding to ViewModel-->
    <ContentPage.BindingContext>
        <viewmodels:PrizeHistoryViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="PrizeList" ItemsSource="{Binding Prizes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Points}"/>
                        
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Header>
                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Center" Text="Your Prize History"/>
                    </StackLayout>
                </ListView.Header>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>